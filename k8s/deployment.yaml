apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simple-api
  template:
    metadata:
      labels:
        app: simple-api
    spec:
      containers:
        - name: simple-api
          image: dantunes/simple-api
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: 10Mi
              cpu: 1m
            limits:
              memory: 10Mi
              cpu: 1m

        
    